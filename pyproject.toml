[project]
name = "veridex"
version = "0.0.0"
description = "Veridex — TxLINE Agent Proof Arena (Phase 0 spike)"
requires-python = ">=3.11"
dependencies = ["pydantic>=2"]

[dependency-groups]
dev = ["pytest>=8"]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
addopts = "-q"
