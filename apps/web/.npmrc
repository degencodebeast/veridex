# eslint-config-next pulls in @rushstack/eslint-patch, which walks the require
# stack expecting a hoisted node_modules layout. pnpm's symlinked store breaks
# that ("Failed to patch ESLint…"); hoist eslint packages so the patch resolves.
public-hoist-pattern[]=*eslint*
public-hoist-pattern[]=@rushstack/*
