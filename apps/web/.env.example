# auth-contract@1 (frontend): the Privy app ID. Without this set, AuthProvider renders the app
# unauthenticated (no PrivyProvider mount) — the auth seam stays fail-closed, so owner-scoped
# calls (e.g. deployAgent) still refuse to fire; only the login UI is skipped.
NEXT_PUBLIC_PRIVY_APP_ID=
